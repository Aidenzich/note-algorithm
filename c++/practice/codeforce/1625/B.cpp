#include <iostream>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int round = 0;
    cin >> round;

    int len = 0;
    
    while(round--){
        cin >> len;
        int input[len];
        int resultLen = 0;
        for (int i=0; i< len; i++){
            cin >> input[i];
        }
        
        for (int i=(len/2); i<len; i++){
            input
        }
    }

    return 0;

}